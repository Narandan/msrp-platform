// ─── CONFIG ──────────────────────────────────────────────────────────────────
// Refactoring: Due to weird runtime errors, config will temporarily be moved to its own file.
// Also, I am attempting to solve a localhost problem, so it will be localhost instead of 127.0... for now.
const API = "http://localhost:8000";

export { API };